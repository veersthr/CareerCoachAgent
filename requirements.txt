python-dotenv==1.2.3
pydantic==2.13.4
tenacity==9.1.4
requests==2.34.2

# LLM providers — install only the one(s) matching your LLM_PROVIDER setting
groq==1.6.0
google-generativeai==0.8.3

# Local embeddings + vector search
sentence-transformers==6.0.0
chromadb==1.5.9
faiss-cpu==1.9.0

# PDF / OCR — Tesseract binary must also be installed separately (e.g. `brew install tesseract`)
pymupdf==1.28.2
pytesseract==0.3.13
Pillow==12.3.0

# Orchestration
langgraph==1.2.11
langgraph-checkpoint==4.2.0
langgraph-checkpoint-sqlite==3.1.1

# API
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32

# dev/test only — used by the api.py test suite (TestClient)
httpx==0.28.1
